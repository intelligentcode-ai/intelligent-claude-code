[defaults]
host_key_checking = False
gathering = smart
interpreter_python = auto_silent
# Using default callback for cross-platform compatibility
# Debian compatibility: yaml callback causes SafeRepresenter AttributeError
stdout_callback = default
deprecation_warnings = False

[ssh_connection]
pipelining = True